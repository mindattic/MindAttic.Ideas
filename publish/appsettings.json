{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",

  "Media": {
    "//": "MAI-A31. Backing store for /_media/{uid}. 'local' keeps assets on disk under {ContentRoot}/media;",
    "//azure": "'azure' streams them to Blob Storage and 302s to a short-lived SAS so Azure serves Range/seek.",
    "//creds": "Credentials belong in the Vault 'Media' bucket, not here (HOUSE-LAW-3). azure fails closed without one.",
    "Provider": "local",
    "Azure": {
      "//ConnectionString": "Account connection string; enables key-signed SAS. Or set BlobServiceUri for Entra.",
      "//BlobServiceUri": "https://<account>.blob.core.windows.net — authenticated with DefaultAzureCredential.",
      "//PublicRead": "true when the container allows anonymous reads: hands out a plain, CDN-cacheable URL instead of a SAS.",
      "ContainerName": "media",
      "SignedUrlMinutes": 60
    }
  },

  "MindAttic": {
    "Auth": {
      "Mfa": {
        "//": "MFA is OFF for now (FOUNDATION_AMENDMENTS A16). RequireForAdmin=false stops the forced",
        "//enroll": "/account/mfa/setup redirect and makes MaPolicies.Admin role-only. Flip to true to re-enable.",
        "RequireForAdmin": false
      }
    }
  }
}
