{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "//Showroom": [
    "Showroom mode (MAI-A38). Enabled=false keeps the idle SWEEP off, so nothing is deleted while you",
    "work — the baseline is still configured, so an on-demand reset (the Sites panel button, or",
    "--reset-sandbox <key>) restores Day Zero. The path is relative to the content root, which in dev",
    "is src/MindAttic.Ideas.Blazor, hence the climb to the repo-root seed/ directory."
  ],
  "Showroom": {
    "Enabled": false,
    "IntervalMinutes": 2,
    "BaselineBundle": "../../seed/mindattic-site.ideabundle"
  }
}
