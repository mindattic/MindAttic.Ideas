@namespace MindAttic.Ideas.Theme.Bootstrap
@inherits ThemeBase

@* MindAttic.Ideas.Theme.Bootstrap.V1 — the host's tiny built-in fallback theme, so a page renders
   before any real theme is installed. Chrome + ONE @Body hole. No zones. *@
<div class="ma-bootstrap-theme">
    @Body
</div>
