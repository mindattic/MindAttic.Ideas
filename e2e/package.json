{
  "name": "mindattic-ideas-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "Cypress end-to-end tests for the MindAttic.Ideas admin flow (login → add page → upload .idea → reference by {{tag}} → renders).",
  "scripts": {
    "cy:open": "cypress open",
    "cy:run": "cypress run"
  },
  "devDependencies": {
    "cypress": "^13.15.0"
  }
}
